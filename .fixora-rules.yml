rules:
- id: fixora-wrapper-get-user
  patterns:
  - pattern-inside: "def get_user(...):\n  ..."
  - pattern-either:
    - pattern: sqlite3.connect(...)
    - pattern: $OBJ.connect(...)
    - pattern: connect(...)
    - pattern: conn.cursor(...)
    - pattern: $OBJ.cursor(...)
    - pattern: cursor(...)
    - pattern: cursor.execute(...)
    - pattern: $OBJ.execute(...)
    - pattern: execute(...)
  message: 'SQL Injection via `user_id`

    Vulnerable Sink: Passes untrusted data directly to `sqlite3.connect, conn.cursor,
    cursor.execute`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: SQL Injection
    confidence: HIGH
    wrapper_defined_in: backend/easy.py
    wraps:
    - sqlite3.connect
    - conn.cursor
    - cursor.execute
    modules_used:
    - flask
    - sqlite3
    vulnerable_parameter: user_id
    malicious_payload: 1 OR 1=1 --
    exploit_explanation: The attacker-controlled user_id is concatenated into the
      SQL WHERE clause, so the payload converts the predicate into an always-true
      condition.
    exploit_injected_example: get_user(user_id = 1 OR 1=1 --)
    impact_summary: Attackers can exfiltrate all rows from the table and bypass authorization
      checks tied to user scoping.
    cwe: &id003
    - CWE-89
    owasp: &id004
    - A03:2021
- id: fixora-wrapper-ping-server
  patterns:
  - pattern-inside: "def ping_server(...):\n  ..."
  - pattern-either:
    - pattern: os.system(...)
    - pattern: $OBJ.system(...)
    - pattern: system(...)
  message: 'Command Injection via `ip_address`

    Vulnerable Sink: Passes untrusted data directly to `os.system`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Command Injection
    confidence: HIGH
    wrapper_defined_in: backend/easy.py
    wraps:
    - os.system
    modules_used:
    - flask
    - os
    vulnerable_parameter: ip_address
    malicious_payload: ; rm -rf /
    exploit_explanation: The attacker-controlled ip_address is concatenated into the
      system command, allowing arbitrary command execution.
    exploit_injected_example: ping_server(ip_address = ; rm -rf /)
    impact_summary: Attackers can execute arbitrary system commands, potentially leading
      to system compromise.
    cwe: &id001
    - CWE-78
    owasp: &id002
    - A03:2021
- id: fixora-wrapper-read-log
  patterns:
  - pattern-inside: "def read_log(...):\n  ..."
  - pattern-either:
    - pattern: open(...)
    - pattern: $OBJ.open(...)
  message: 'Path Traversal via `filename`

    Vulnerable Sink: Passes untrusted data directly to `open`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Path Traversal
    confidence: HIGH
    wrapper_defined_in: backend/easy.py
    wraps:
    - open
    modules_used:
    - flask
    - builtins
    vulnerable_parameter: filename
    malicious_payload: ../../../../etc/passwd
    exploit_explanation: The attacker-controlled filename is concatenated into the
      file path, allowing access to arbitrary files.
    exploit_injected_example: read_log(filename = ../../../../etc/passwd)
    impact_summary: Attackers can read arbitrary files on the system, potentially
      leading to sensitive data exposure.
    cwe:
    - CWE-22
    owasp:
    - A01:2021
- id: fixora-wrapper-import-data
  patterns:
  - pattern-inside: "def import_data(...):\n  ..."
  - pattern-either:
    - pattern: pickle.loads(...)
    - pattern: $OBJ.loads(...)
    - pattern: loads(...)
  message: 'Insecure Deserialization via `data`

    Vulnerable Sink: Passes untrusted data directly to `pickle.loads`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Insecure Deserialization
    confidence: HIGH
    wrapper_defined_in: backend/easy.py
    wraps:
    - pickle.loads
    modules_used:
    - pickle
    vulnerable_parameter: data
    malicious_payload: load_payload("gASV...crafted_pickle...")
    exploit_explanation: The attacker-controlled data is deserialized using pickle,
      allowing arbitrary code execution.
    exploit_injected_example: import_data(data = load_payload("gASV...crafted_pickle..."))
    impact_summary: Attackers can execute arbitrary code, potentially leading to system
      compromise.
- id: fixora-wrapper-login
  patterns:
  - pattern-inside: "def login(...):\n  ..."
  - pattern-either:
    - pattern: data.get(...)
    - pattern: $OBJ.get(...)
    - pattern: get(...)
  message: 'Plaintext Password via `password`

    Vulnerable Sink: Passes untrusted data directly to `data.get`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Plaintext Password
    confidence: HIGH
    wrapper_defined_in: backend/easy.py
    wraps:
    - data.get
    modules_used:
    - flask
    vulnerable_parameter: password
    malicious_payload: ''
    exploit_explanation: The password is compared in plaintext, allowing attackers
      to obtain the password.
    exploit_injected_example: ''
    impact_summary: Attackers can obtain the password, potentially leading to unauthorized
      access.
    cwe:
    - CWE-256
    - CWE-916
    owasp:
    - A02:2021
- id: fixora-wrapper-process-data
  patterns:
  - pattern-inside: "def process_data(...):\n  ..."
  - pattern-either:
    - pattern: subprocess.Popen(...)
    - pattern: $OBJ.Popen(...)
    - pattern: Popen(...)
  message: 'Command Injection via `filename`

    Vulnerable Sink: Passes untrusted data directly to `subprocess.Popen`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Command Injection
    confidence: HIGH
    wrapper_defined_in: backend/hard.py
    wraps:
    - subprocess.Popen
    modules_used:
    - flask
    - subprocess
    vulnerable_parameter: filename
    malicious_payload: ; rm -rf /
    exploit_explanation: The attacker-controlled filename is interpolated into the
      system command, allowing arbitrary command execution.
    exploit_injected_example: process_data(filename = ; rm -rf /)
    impact_summary: Attackers can execute arbitrary system commands, potentially leading
      to system compromise.
    cwe: *id001
    owasp: *id002
- id: fixora-wrapper-reset-system
  patterns:
  - pattern-inside: "def reset_system(...):\n  ..."
  - pattern-either:
    - pattern: sqlite3.connect(...)
    - pattern: $OBJ.connect(...)
    - pattern: connect(...)
    - pattern: conn.execute(...)
    - pattern: $OBJ.execute(...)
    - pattern: execute(...)
  message: 'SQL Injection via `table_name`

    Vulnerable Sink: Passes untrusted data directly to `sqlite3.connect, conn.execute`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: SQL Injection
    confidence: HIGH
    wrapper_defined_in: backend/hard.py
    wraps:
    - sqlite3.connect
    - conn.execute
    modules_used:
    - flask
    - sqlite3
    vulnerable_parameter: table_name
    malicious_payload: users; DROP TABLE users --
    exploit_explanation: The attacker-controlled table_name is interpolated into the
      SQL query, allowing arbitrary SQL execution.
    exploit_injected_example: reset_system(table_name = users; DROP TABLE users --)
    impact_summary: Attackers can execute arbitrary SQL queries, potentially leading
      to data tampering or deletion.
    cwe: *id003
    owasp: *id004
- id: fixora-wrapper-view-profile
  patterns:
  - pattern-inside: "def view_profile(...):\n  ..."
  - pattern-either:
    - pattern: sqlite3.connect(...)
    - pattern: $OBJ.connect(...)
    - pattern: connect(...)
    - pattern: conn.cursor(...)
    - pattern: $OBJ.cursor(...)
    - pattern: cursor(...)
    - pattern: cursor.execute(...)
    - pattern: $OBJ.execute(...)
    - pattern: execute(...)
  message: 'SQL Injection via `bio`

    Vulnerable Sink: Passes untrusted data directly to `sqlite3.connect, conn.cursor,
    cursor.execute`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: SQL Injection
    confidence: HIGH
    wrapper_defined_in: backend/hard.py
    wraps:
    - sqlite3.connect
    - conn.cursor
    - cursor.execute
    modules_used:
    - sqlite3
    vulnerable_parameter: bio
    malicious_payload: 1' OR 1=1 --
    exploit_explanation: The attacker-controlled bio is concatenated into the SQL
      query, allowing arbitrary SQL execution.
    exploit_injected_example: view_profile(bio = 1' OR 1=1 --)
    impact_summary: Attackers can execute arbitrary SQL queries, potentially leading
      to data tampering or deletion.
    cwe: *id003
    owasp: *id004
- id: fixora-wrapper-search-users
  patterns:
  - pattern-inside: "def search_users(...):\n  ..."
  - pattern-either:
    - pattern: db.users.find(...)
    - pattern: $OBJ.find(...)
    - pattern: find(...)
  message: 'SQL Injection via `query`

    Vulnerable Sink: Passes untrusted data directly to `db.users.find`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: SQL Injection
    confidence: HIGH
    wrapper_defined_in: backend/hard.py
    wraps:
    - db.users.find
    modules_used:
    - pymongo
    vulnerable_parameter: query
    malicious_payload: '{"username": {"$gt": ""}}'
    exploit_explanation: The attacker-controlled query is executed directly on the
      database, allowing arbitrary data retrieval.
    exploit_injected_example: 'search_users(query = {"username": {"$gt": ""}})'
    impact_summary: Attackers can retrieve arbitrary data from the database, potentially
      leading to sensitive data exposure.
    cwe: *id003
    owasp: *id004
- id: fixora-wrapper-set-config
  patterns:
  - pattern-inside: "def set_config(...):\n  ..."
  - pattern-either:
    - pattern: setattr(...)
    - pattern: $OBJ.setattr(...)
  message: 'Security Misconfiguration via `data`

    Vulnerable Sink: Passes untrusted data directly to `setattr`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Security Misconfiguration
    confidence: HIGH
    wrapper_defined_in: backend/hard.py
    wraps:
    - setattr
    modules_used:
    - builtins
    vulnerable_parameter: data
    malicious_payload: '{"__class__": "Admin"}'
    exploit_explanation: The attacker-controlled data is used to override server-side
      attributes, allowing arbitrary attribute modification.
    exploit_injected_example: 'set_config(data = {"__class__": "Admin"})'
    impact_summary: Attackers can modify arbitrary server-side attributes, potentially
      leading to security vulnerabilities or data tampering.
- id: fixora-wrapper-update-settings
  patterns:
  - pattern-inside: "def update_settings(...):\n  ..."
  - pattern-either:
    - pattern: sqlite3.connect(...)
    - pattern: $OBJ.connect(...)
    - pattern: connect(...)
    - pattern: conn.execute(...)
    - pattern: $OBJ.execute(...)
    - pattern: execute(...)
  message: 'IDOR / Broken Access Control via `target_user_id`

    Vulnerable Sink: Passes untrusted data directly to `sqlite3.connect, conn.execute`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: IDOR / Broken Access Control
    confidence: HIGH
    wrapper_defined_in: backend/hard.py
    wraps:
    - sqlite3.connect
    - conn.execute
    modules_used:
    - flask
    - sqlite3
    vulnerable_parameter: target_user_id
    malicious_payload: ''
    exploit_explanation: The attacker-controlled target_id is used to update settings,
      allowing arbitrary user settings modification.
    exploit_injected_example: ''
    impact_summary: Attackers can modify arbitrary user settings, potentially leading
      to security vulnerabilities or data tampering.
- id: fixora-wrapper-verify
  patterns:
  - pattern-inside: "def verify(...):\n  ..."
  - pattern-either:
    - pattern: hmac.new(...)
    - pattern: $OBJ.new(...)
    - pattern: new(...)
  message: 'Security Misconfiguration via `signature`

    Vulnerable Sink: Passes untrusted data directly to `hmac.new`.'
  severity: WARNING
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Security Misconfiguration
    confidence: MEDIUM
    wrapper_defined_in: backend/hard.py
    wraps:
    - hmac.new
    modules_used:
    - hmac
    vulnerable_parameter: signature
    malicious_payload: <malicious_data>
    exploit_explanation: The insecure comparison allows attackers to exploit timing
      differences, potentially leading to signature guessing.
    exploit_injected_example: verify(signature = <malicious_data>)
    impact_summary: Attackers can exploit timing differences to guess the signature,
      potentially leading to security vulnerabilities.
- id: fixora-wrapper-trigger-webhook
  patterns:
  - pattern-inside: "def trigger_webhook(...):\n  ..."
  - pattern-either:
    - pattern: requests.get(...)
    - pattern: $OBJ.get(...)
    - pattern: get(...)
  message: 'SSRF via `url`

    Vulnerable Sink: Passes untrusted data directly to `requests.get`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: SSRF
    confidence: HIGH
    wrapper_defined_in: backend/medium.py
    wraps:
    - requests.get
    modules_used:
    - flask
    - requests
    vulnerable_parameter: url
    malicious_payload: http://localhost:8080
    exploit_explanation: The attacker-controlled target_url is used to make a request,
      allowing arbitrary request execution.
    exploit_injected_example: trigger_webhook(url = http://localhost:8080)
    impact_summary: Attackers can execute arbitrary requests, potentially leading
      to security vulnerabilities or data tampering.
    cwe:
    - CWE-918
    owasp:
    - A10:2021
- id: fixora-wrapper-upload-xml
  patterns:
  - pattern-inside: "def upload_xml(...):\n  ..."
  - pattern-either:
    - pattern: etree.fromstring(...)
    - pattern: $OBJ.fromstring(...)
    - pattern: fromstring(...)
  message: 'Security Misconfiguration via `xml_data`

    Vulnerable Sink: Passes untrusted data directly to `etree.fromstring`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Security Misconfiguration
    confidence: HIGH
    wrapper_defined_in: backend/medium.py
    wraps:
    - etree.fromstring
    modules_used:
    - lxml
    vulnerable_parameter: xml_data
    malicious_payload: <!ENTITY xxe SYSTEM 'file:///etc/passwd'>
    exploit_explanation: The insecure XML parsing allows attackers to exploit external
      entities, potentially leading to arbitrary file access.
    exploit_injected_example: upload_xml(xml_data = <!ENTITY xxe SYSTEM 'file:///etc/passwd'>)
    impact_summary: Attackers can access arbitrary files, potentially leading to sensitive
      data exposure.
- id: fixora-wrapper-validate
  patterns:
  - pattern-inside: "def validate(...):\n  ..."
  - pattern-either:
    - pattern: re.match(...)
    - pattern: $OBJ.match(...)
    - pattern: match(...)
  message: 'Input Validation Failure via `email`

    Vulnerable Sink: Passes untrusted data directly to `re.match`.'
  severity: WARNING
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Input Validation Failure
    confidence: MEDIUM
    wrapper_defined_in: backend/medium.py
    wraps:
    - re.match
    modules_used:
    - flask
    - re
    vulnerable_parameter: email
    malicious_payload: a" * 10000
    exploit_explanation: The insecure regular expression allows attackers to exploit
      catastrophic backtracking, potentially leading to denial-of-service.
    exploit_injected_example: validate(email = a" * 10000)
    impact_summary: Attackers can exploit catastrophic backtracking, potentially leading
      to denial-of-service.
    cwe:
    - CWE-400
    - CWE-1333
    owasp:
    - A03:2021
- id: fixora-wrapper-update
  patterns:
  - pattern-inside: "def update(...):\n  ..."
  - pattern-either:
    - pattern: setattr(...)
    - pattern: $OBJ.setattr(...)
  message: 'Mass Assignment via `**kwargs`

    Vulnerable Sink: Passes untrusted data directly to `setattr`.'
  severity: ERROR
  languages:
  - python
  metadata:
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Mass Assignment
    confidence: HIGH
    wrapper_defined_in: backend/medium.py
    wraps:
    - setattr
    modules_used:
    - builtins
    vulnerable_parameter: '**kwargs'
    malicious_payload: '{"__class__": "Admin"}'
    exploit_explanation: The attacker-controlled data is used to update attributes,
      allowing arbitrary attribute modification.
    exploit_injected_example: 'update(**kwargs = {"__class__": "Admin"})'
    impact_summary: Attackers can modify arbitrary attributes, potentially leading
      to security vulnerabilities or data tampering.
    cwe:
    - CWE-915
    owasp:
    - A03:2021
- id: fixora-wrapper-useeffect
  patterns:
  - pattern-either:
    - pattern: function useEffect(...) { ... }
    - pattern: const useEffect = (...) => { ... }
    - pattern: let useEffect = (...) => { ... }
    - pattern: var useEffect = (...) => { ... }
    - pattern: this.useEffect = (...) => { ... }
    - pattern: this.useEffect = function(...) { ... }
    - pattern: 'useEffect: (...) => { ... }'
    - pattern: 'useEffect: function(...) { ... }'
    - pattern: useEffect(...) { ... }
    - pattern: useEffect = (...) => { ... }
    - pattern: useEffect = function(...) { ... }
  - pattern-either:
    - pattern: setTimeout(...)
    - pattern: $OBJ.setTimeout(...)
  message: 'XSS via `hash`

    Vulnerable Sink: Passes untrusted data directly to `setTimeout`.'
  severity: ERROR
  languages:
  - javascript
  - typescript
  metadata:
    category: security
    technology:
    - javascript
    - typescript
    source: fixora-ai-analysis
    vulnerability_type: XSS
    confidence: HIGH
    wrapper_defined_in: frontend/components.jsx
    wraps:
    - setTimeout
    modules_used:
    - builtins
    vulnerable_parameter: hash
    malicious_payload: javascript:alert('XSS')
    exploit_explanation: The attacker-controlled hash is executed directly by setTimeout,
      allowing for arbitrary JavaScript execution.
    exploit_injected_example: useEffect(hash = javascript:alert('XSS'))
    impact_summary: Attackers can execute arbitrary JavaScript code, potentially leading
      to sensitive data theft or unauthorized actions.
    cwe: &id005
    - CWE-79
    owasp: &id006
    - A03:2021
- id: fixora-wrapper-userprofile
  patterns:
  - pattern-either:
    - pattern: function UserProfile(...) { ... }
    - pattern: const UserProfile = (...) => { ... }
    - pattern: let UserProfile = (...) => { ... }
    - pattern: var UserProfile = (...) => { ... }
    - pattern: this.UserProfile = (...) => { ... }
    - pattern: this.UserProfile = function(...) { ... }
    - pattern: 'UserProfile: (...) => { ... }'
    - pattern: 'UserProfile: function(...) { ... }'
    - pattern: UserProfile(...) { ... }
    - pattern: UserProfile = (...) => { ... }
    - pattern: UserProfile = function(...) { ... }
  - pattern-either:
    - pattern: dangerouslySetInnerHTML(...)
    - pattern: $OBJ.dangerouslySetInnerHTML(...)
  message: 'XSS via `userData.bio`

    Vulnerable Sink: Passes untrusted data directly to `dangerouslySetInnerHTML`.'
  severity: ERROR
  languages:
  - javascript
  - typescript
  metadata:
    category: security
    technology:
    - javascript
    - typescript
    source: fixora-ai-analysis
    vulnerability_type: XSS
    confidence: HIGH
    wrapper_defined_in: frontend/components.jsx
    wraps:
    - dangerouslySetInnerHTML
    modules_used:
    - react
    - React/DOM
    vulnerable_parameter: userData.bio
    malicious_payload: <script>alert('XSS')</script>
    exploit_explanation: The attacker-controlled userData.bio is rendered directly
      into the DOM, allowing for arbitrary JavaScript execution.
    exploit_injected_example: UserProfile(userData.bio = <script>alert('XSS')</script>)
    impact_summary: Attackers can execute arbitrary JavaScript code, potentially leading
      to sensitive data theft or unauthorized actions.
    cwe: *id005
    owasp: *id006
- id: fixora-wrapper-livesearch
  patterns:
  - pattern-either:
    - pattern: function LiveSearch(...) { ... }
    - pattern: const LiveSearch = (...) => { ... }
    - pattern: let LiveSearch = (...) => { ... }
    - pattern: var LiveSearch = (...) => { ... }
    - pattern: this.LiveSearch = (...) => { ... }
    - pattern: this.LiveSearch = function(...) { ... }
    - pattern: 'LiveSearch: (...) => { ... }'
    - pattern: 'LiveSearch: function(...) { ... }'
    - pattern: LiveSearch(...) { ... }
    - pattern: LiveSearch = (...) => { ... }
    - pattern: LiveSearch = function(...) { ... }
  - pattern-either:
    - pattern: fetch(...)
    - pattern: $OBJ.fetch(...)
  message: 'Business Logic Flaw via `fetch response`

    Vulnerable Sink: Passes untrusted data directly to `fetch`.'
  severity: WARNING
  languages:
  - javascript
  - typescript
  metadata:
    category: security
    technology:
    - javascript
    - typescript
    source: fixora-ai-analysis
    vulnerability_type: Business Logic Flaw
    confidence: MEDIUM
    wrapper_defined_in: frontend/components.jsx
    wraps:
    - fetch
    modules_used:
    - builtins
    vulnerable_parameter: fetch response
    malicious_payload: <malicious_data>
    exploit_explanation: The application sends multiple requests rapidly, potentially
      leading to inconsistent state.
    exploit_injected_example: LiveSearch(fetch response = <malicious_data>)
    impact_summary: Attackers can potentially exploit the inconsistent state to perform
      unauthorized actions or access sensitive data.
